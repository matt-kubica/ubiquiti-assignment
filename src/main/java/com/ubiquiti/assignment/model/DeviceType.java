package com.ubiquiti.assignment.model;


/**
 * An enum to classify different types of networking devices.
 * Order of the enum items is intentional, and it's necessary for proper sorting of networking devices.
 */
public enum DeviceType {
    GATEWAY, SWITCH, ACCESS_POINT
}
